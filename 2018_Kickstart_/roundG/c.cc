#include <stdio>

int main(void) {
    int T;
    scanf("%d", &T);
    for(int t = 0; t < T; t++) {
        int N, M, E, SR, SC, TR, TC;
        int map[101][101];
        scanf("%d %d %d %d %d %d %d", &N, &M, &SR, &SC, &TR, &TC);
        for(int i = 0; i < N; i++) {
            for(int j = 0; j < M; j++) {
                scanf("%d", &map[i][j]);
            }
        }


    }
}
